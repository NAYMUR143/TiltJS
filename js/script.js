VanillaTilt.init(document.querySelector(".card"), {
  max: 20,
  speed: 600,
  scale: 1.1,
  transition: true,
  easing: "cubic-bezier(.03,.98,.52,.99)",
  perspective: 400,
  glare: true,
});
